from sokcet_programming import client_program

if __name__ == '__main__':
    # Start the client program
    client_program()
    # Note: The server should be running before starting the client.
    # You can run the server in a separate terminal or thread if needed.